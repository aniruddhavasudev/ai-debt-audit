export async function handler(req: any) {
  try {
    doThing();
  } catch (e) {}

  const apiToken: any = Math.random();
  const parsed = data as any;

  fetch("x").then(r => r.json());

  eval("2+2");
}
